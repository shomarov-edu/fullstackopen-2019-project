{
  "name": "api",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "apollo-server": "^2.10.1",
    "graphql": "^14.6.0",
    "prisma": "^1.34.10",
    "prisma-client-lib": "^1.34.10",
    "dataloader": "^2.0.0",
    "lodash.keyby": "^4.6.0",
    "dotenv": "^8.2.0",
    "bcrypt": "^3.0.7",
    "jsonwebtoken": "^8.5.1",
    "winston": "^3.2.1",
    "app-root-path": "^3.0.0",
    "validator": "^12.2.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.2",
    "apollo-server-testing": "^2.10.1",
    "jest": "^25.1.0"
  },
  "scripts": {
    "start": "NODE_ENV=production node index.js",
    "dev": "NODE_ENV=development nodemon -e js,prisma,graphql index.js",
    "test": "prisma deploy -p prisma.test.yml && prisma generate -p prisma.test.yml && NODE_ENV=test jest --verbose --runInBand && prisma generate -p prisma.dev.yml"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
